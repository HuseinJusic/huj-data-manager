const initState = {
    datasets: null
}

const DatasetReducer = (state = initState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default DatasetReducer;